# Hermes OpenRouter Sandbox Workspace

This directory is mounted into the container as `/workspace`.

Use it for safe coding/tool tests. The sandbox Hermes config sets `terminal.cwd` to `/workspace`.
